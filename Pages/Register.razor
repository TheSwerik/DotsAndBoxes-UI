@page "/register"

<div class="container">
    <div class="center">
        <div class="card card-block mx-auto p-4 align-items-center">
            <EditForm Model="@AuthenticateModel" OnValidSubmit="OpenServerBrowser">
                <DataAnnotationsValidator/>
                <ValidationSummary/>
                <table class="form-group">
                    <tr>
                        <td>Username:</td>
                        <td>
                            <InputText id="name" class="form-control" @bind-Value="AuthenticateModel.Username"/>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            Password:
                        </td>
                        <td>
                            <InputText type="password" id="password" class="form-control" @bind-Value="AuthenticateModel.Password"/>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            Confirm Password:
                        </td>
                        <td>
                            <InputText type="password" id="confirmpassword" class="form-control" @bind-Value="ConfirmPassword"/>
                        </td>
                    </tr>
                </table>
                <button type="submit" class="form-control btn-primary">Register</button>
            </EditForm>
        </div>
    </div>
</div>